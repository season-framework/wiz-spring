# Company template notes

This project was generated as `__WIZ_TEMPLATE_ID__` from the `__WIZ_BASE_TEMPLATE__` build contract.

Keep `package.json` metadata aligned with the base template while customizing application source and instructions.
