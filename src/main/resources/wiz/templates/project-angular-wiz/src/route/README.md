# Frontend routes

Page routes are generated from each `src/app/*/app.json` `viewuri`. Keep additional route metadata in this directory when the application needs it; backend HTTP routes belong in standard Spring controllers.
