# Portal sources

Reusable WIZ portals live below this directory. A portal may provide `app/`, `assets/`, and `libs/` directories. `npm run wizbuild` stages them with the root WIZ frontend without changing these sources.
