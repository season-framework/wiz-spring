# Portal Styles Guide

Portal package 스타일은 app-local `view.scss`, package `styles/`, Angular 전역 styles의 우선순위를 명확히 둔다.

- component 단위 스타일은 해당 app의 `view.scss`
- package 전역 스타일은 `src/portal/{package}/styles`
- 전체 application theme은 `src/angular/styles`

빌드 산출물 CSS를 직접 수정하지 않는다.
