# Portal Assets Guide

Portal asset은 `src/portal/{package}/assets`에 둔다. 로고, icon, font, package 전용 이미지처럼 package와 함께 이동해야 하는 파일을 배치한다.

private file access나 download 권한 검사가 필요하면 static asset으로 직접 노출하지 말고 route handler를 작성한다.
