spring:
  application:
    name: wiz-spring

server:
  address: 127.0.0.1
  port: 3000

management:
  endpoints:
    web:
      exposure:
        include: health,info

wiz:
  root: .
  project:
    default-name: main
